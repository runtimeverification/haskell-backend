module ID

  import INT []

  sort Id{} []

  symbol id{}(Int{}) : Id{} []

  /* id is injective. */

  axiom{}
    \implies{Id{}}(
      \and{Id{}}(id{}(I1:Int{}), id{}(I2:Int{})),
      id{}(\and{Int{}}(I1:Int{}, I2:Int{})))
  []

endmodule []
