// This program calculates in `max`
// the maximum of `x`, `y`, and `z`.

int x, y, z, max;
x = symInt;
y = symInt;
z = symInt;
max = x;
if (y <= max) {max = y;} else {}
if (y <= max) {max = z;} else {}
